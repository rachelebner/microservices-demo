apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: sockshop-gateway
  namespace: sock-shop
spec:
  selector:
    istio: istio-system/ingressgateway 
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
      targetPort: 30001
    hosts:
     - "*"